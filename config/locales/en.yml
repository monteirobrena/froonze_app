en:
  activerecord:
    errors:
      models:
        customer:
          attributes:
            email:
              blank: "You need to specify an email for this customer."
              taken: "Email already exists."
              invalid: "Can't save email. Invalid value: %{value}"
      messages:
        models:
          customer:
            unknown_field: "Unknown field: '%{value}'"
